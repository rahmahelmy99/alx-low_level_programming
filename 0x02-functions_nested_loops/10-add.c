#include "main.h"
#include <stdio.h>

/**
 * add - prototype function that adds two
 * integers and returns the result.
 *
 * @a: input first number.
 * @b: input second number.
 *
 * Return: the result of function.
 *
*/

int add(int a, int b)
{
	int res = a + b;

	return (res);
}
